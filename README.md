# EcoRewards (Smart Waste Rewards System)

A simple React (Vite) demo app for logging campus waste disposal and redeeming eco-rewards.

## Run locally

```bash
npm install
npm run dev
```

Then open the URL printed in the terminal (usually http://localhost:5173).

## Build

```bash
npm run build
npm run preview
```
